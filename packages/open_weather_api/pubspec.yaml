name: open_weather_api
description: A Dart API Client for the Open Weather API.
version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.4
  json_annotation: ^4.0.0
  flutter_dotenv: ^5.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  coverage: ^1.0.3
  build_runner: ^1.10.0
  json_serializable: ^4.0.0
  mocktail: ^0.1.0
  test: ^1.16.4